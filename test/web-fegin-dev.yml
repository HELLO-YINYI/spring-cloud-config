spring:
  application:
    name: spring-cloud-web-feign
  boot:
    admin:
      client:
        url: http://localhost:8181

server:
  port: 6602

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8801/eureka/
feign:
  hystrix:
    enabled: true